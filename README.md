# tilesetcollisiongenerator
A plugin for Godot 3.x that generates collision for a tileset in based on the textures

If you can't see any collisions after generating them, try refreshing your project.
